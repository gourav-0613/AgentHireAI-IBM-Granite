"""
utils package

Pure utility functions with no external dependencies beyond the standard
library.  No Streamlit, no Watsonx, no Pydantic imports at the package level.

Modules:
    text_cleaner  — normalise and sanitise raw text extracted from PDFs
    validators    — input guard helpers (file type, file size, text length)
"""
