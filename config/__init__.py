"""
config package

Exposes application settings and prompt templates to the rest of the project.
Import directly from sub-modules:
    from config.settings import settings
    from config.prompts import RESUME_PARSER_PROMPT
"""
